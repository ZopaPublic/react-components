import styled from 'styled-components';
import Heading from '../../../atoms/Heading/Heading';

export const FooterHeading = styled(Heading).attrs({ as: 'h3', size: 'h6' })`
  text-transform: uppercase;
`;
